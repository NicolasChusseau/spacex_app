name: spacex_app
description: "Une super description de l'application"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: 1.5.0
  json_serializable: 6.11.1
  json_annotation: ^4.9.0
  build_runner: 2.9.0
  flutter_bloc: 8.1.6
  intl: ^0.17.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
